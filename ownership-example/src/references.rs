pub(crate) fn references_main() {
    //TO DO: Create a function that uses references and the one that doesn't but uses plain values
    // Connected to docs book 4.2

    println!("Hello from References")
}